import { ExactLengthValidator } from './exact-length-validator';

describe('ExactLengthValidator', () => {
  it('should create an instance', () => {
    expect(new ExactLengthValidator()).toBeTruthy();
  });
});
