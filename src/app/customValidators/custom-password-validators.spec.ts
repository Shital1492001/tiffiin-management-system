import { CustomPasswordValidators } from './custom-password-validators';

describe('CustomPasswordValidators', () => {
  it('should create an instance', () => {
    expect(new CustomPasswordValidators()).toBeTruthy();
  });
});
